.DS_Store
node_modules/
inject-beats.js
inject-lyrics.js
users.json
secret.txt

# User data — live shows are mutable data, not code. Seeded from app/seed-shows/.
app/shows/
app/snapshots/

# Source art / design files dropped at the repo root (built icons live in app/).
/*.png
